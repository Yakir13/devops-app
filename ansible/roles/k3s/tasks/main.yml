---
# tasks file for roles/k3n
- name: Install K3s
  shell: curl -sfL https://get.k3s.io | sh -
  args:
    creates: /usr/local/bin/k3s

- name: Make kubeconfig readable
  file:
    path: /etc/rancher/k3s/k3s.yaml
    mode: "0644"